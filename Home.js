import React, { useEffect } from "react";
import Chart from "chart.js/auto";

function Home() {
  useEffect(() => {
    // Animate counters, render charts
    // ...use similar logic as your original JS but inside React's useEffect
  }, []);
  return (
    <div>
      {/* Put all your home page sections here */}
      {/* For example: hero, services chart, counters, reviews preview, blog preview, media preview, etc. */}
    </div>
  );
}

export default Home;